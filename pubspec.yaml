name: dio_logging
description: A simple and fast dio interceptor for logging
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  dio: ^4.0.0
  logging: ^1.1.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
